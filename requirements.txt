torch>=2.0
torchvision
opencv-python-headless
scipy
numpy
pillow
tqdm
# SAM2 — must be installed manually from source:
# git clone https://github.com/facebookresearch/sam2.git && cd sam2 && pip install -e .
